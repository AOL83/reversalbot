# Contributing

Thank you for helping improve ReversalBot.

## Development
1. Install Poetry.
2. Run `poetry install`.
3. Run `poetry run ruff check .` and `poetry run pytest` before submitting.

## Pull Requests
- Keep changes scoped and well-tested.
- Add or update documentation when behavior changes.
